[project]
name = "bigquery-funcs"
version = "0.1.0"
description = "BigQuery functions and/or classes, helper scipts, etc. I commonly use when working with the Python SDK"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-cloud-bigquery>=3.29.0",
]
